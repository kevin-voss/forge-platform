#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "Contract generation placeholder for Step 00."
echo "OpenAPI / Protobuf / event schemas will be generated in later steps."
echo "Contracts root: ${ROOT_DIR}/contracts"
