# Step 50.03: Platform preflight + gateway host routing

## Status

Complete

## Epic

`50-e2e-harness` → [`../../epics/50-e2e-harness.md`](../../epics/50-e2e-harness.md)

## Global sequence (N)

**`N = 176`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `50.03`

## Goal

Guarantee the platform is healthy before any product runs, and make `*.localhost:4000` Gateway
host-routing work for headed/headless browsers.

## Dependencies

* `50.02` runner; `make dev` / compose; all platform services.

## Scope

* `harness/platform.ts`: bring platform up (`make dev` if not running), wait for every service
  `/health`/`/ready` (reuse `scripts/wait-for-service.sh` ports), fail fast with a single blocker
  finding if the platform can't come up.
* `harness/gateway.ts`: helpers to hit `http://127.0.0.1:4000` with `Host:` header (curl-style) and
  to compute product `baseURL`s; verify each product host returns its expected status.
* **Confirm Gateway matches host ignoring `:4000` port.** If it 404s on a port-suffixed Host,
  record a finding and add the documented host-rewrite proxy fallback (harness-only).

## Explicitly out of scope

* Findings *writer* internals (50.04) — this step may emit at most the preflight blocker via a stub.

## Implementation tasks

1. Implement platform up + health-wait with bounded timeout.
2. Implement host preflight for a `hosts[]` list from `demo.json`.
3. Verify/patch the Gateway host-with-port behaviour question; document outcome.

## Files to create

* `tests/e2e/harness/platform.ts`, `harness/gateway.ts`

## Files to modify

* `docs/demo-projects/e2e-harness.md` (record the Gateway host-routing outcome); `progress.md`.

## Unit / integration tests

* Preflight passes against a healthy `make dev`.
* Host preflight against a known service (e.g. Grafana or a demo host) returns expected status.

## Demo scenario

None (foundation).

## Manual verification

```bash
make dev
cd tests/e2e && node -e "require('./harness/platform').preflight()"
curl -fsS -H 'Host: api.taskflow.localhost' http://127.0.0.1:4000/health/ready   # once demo 51 exists
```

## Acceptance criteria

* Preflight blocks until all services healthy or fails with a clear blocker.
* Gateway host routing works for `*.localhost:4000` (directly or via documented fallback).

## Expected Git commit

```text
test(50.03): add platform preflight and gateway host routing
```
