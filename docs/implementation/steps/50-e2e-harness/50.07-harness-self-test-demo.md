# Step 50.07: Harness self-test demo + gate

## Status

Not started

## Epic

`50-e2e-harness` → [`../../epics/50-e2e-harness.md`](../../epics/50-e2e-harness.md)

## Global sequence (N)

**`N = 180`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `50.07`

## Goal

Prove the entire harness end-to-end with a trivial hello-world product: deploy on Forge, open in a
browser, click a button, assert text, record one deliberate sample finding, and tear down.

## Dependencies

* `50.01`–`50.06`.

## Scope

* `demos/50-e2e-harness/`: a minimal static "hello" service with a button, `Dockerfile`,
  `forge.yaml`, resource docs, `run.sh`, `demo.json` (host `hello.localhost`).
* `tests/e2e/projects/50-harness/spec.ts`: click → assert; one `platform.expect` that deliberately
  fails to prove a `minor` sample finding is recorded, then is cleaned from `PLATFORM_FINDINGS.md`
  by the test (or clearly marked `sample`).
* `make demo DEMO=50` runs the full lifecycle and exits 0.

## Explicitly out of scope

* Any real platform capability testing (that is epics 51–55).

## Implementation tasks

1. Build the hello product + manifests + scripts + `demo.json`.
2. Write the spec (product assertion + sample finding).
3. Wire `make demo DEMO=50` through the orchestrator lifecycle.
4. Confirm headed and headless both pass.

## Files to create

* `demos/50-e2e-harness/*` (service, Dockerfile, forge.yaml, run.sh, demo.json, README.md)
* `tests/e2e/projects/50-harness/spec.ts`

## Files to modify

* `Makefile` (demo wiring if needed); `docs/implementation/progress.md`; epic 50 status → Complete.

## Unit / integration tests

* Covered by the demo run itself.

## Demo scenario

```text
deploy hello → open hello.localhost → click → assert "Hello, Forge"
  → record sample finding → report + coverage → teardown → exit 0
```

## Manual verification

```bash
make demo DEMO=50
make test-platform-e2e PROJECTS=50
make test-platform-e2e HEADLESS=1 PROJECTS=50
```

## Acceptance criteria

* `make demo DEMO=50` passes headed and headless; lifecycle + findings + report all exercised.
* Epic 50 marked Complete.

## Expected Git commit

```text
test(50.07): add harness self-test demo and epic 50 gate
```
