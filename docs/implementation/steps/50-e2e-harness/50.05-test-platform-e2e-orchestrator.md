# Step 50.05: `make test-platform-e2e` orchestrator

## Status

Complete

## Epic

`50-e2e-harness` → [`../../epics/50-e2e-harness.md`](../../epics/50-e2e-harness.md)

## Global sequence (N)

**`N = 178`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `50.05`

## Goal

Provide the single entry point `make test-platform-e2e` that preflights the platform and runs each
selected demo product through the full lifecycle in order, aggregating results and exit code.

## Dependencies

* `50.02`–`50.04`.

## Scope

* `harness/orchestrator.ts`: parse `HEADLESS`, `PROJECTS=01,02…` (default all), `KEEP`,
  `FINDINGS_ONLY`; discover products by their `demo.json`; run in numeric order; collect
  pass/degraded/fail + findings + artifacts; exit 0 iff all selected products pass and zero blocker
  findings.
* Make targets `test-platform-e2e`, `e2e-install`, and `demo DEMO=5X` reuse of the lifecycle.

## Explicitly out of scope

* Report formatting/coverage (50.06); full-suite coverage gate (56.02).

## Implementation tasks

1. Implement product discovery + ordered execution + result aggregation.
2. Implement env flags (headless/subset/keep).
3. Add Makefile targets (`test-platform-e2e`, `e2e-install`).
4. Ensure a single product can run standalone via `make demo DEMO=5X`.

## Files to create

* `tests/e2e/harness/orchestrator.ts`

## Files to modify

* `Makefile` (new targets); `docs/implementation/progress.md` (mark 50.05).

## Unit / integration tests

* Orchestrator runs the 50.07 self-test product end-to-end and returns exit 0.
* `PROJECTS=` subset selection and `HEADLESS=1` honored.

## Demo scenario

Full lifecycle proven against the 50.07 hello-world product.

## Manual verification

```bash
make test-platform-e2e PROJECTS=50            # once 50.07 exists
make test-platform-e2e HEADLESS=1 PROJECTS=50
```

## Acceptance criteria

* One command runs selected products in order with correct aggregate exit code.
* Headed default, headless via env; subset + keep flags work.

## Expected Git commit

```text
test(50.05): add test-platform-e2e orchestrator and make targets
```
