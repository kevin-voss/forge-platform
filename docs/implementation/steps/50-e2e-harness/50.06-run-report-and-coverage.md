# Step 50.06: Run report + coverage rollup

## Status

Complete

## Epic

`50-e2e-harness` → [`../../epics/50-e2e-harness.md`](../../epics/50-e2e-harness.md)

## Global sequence (N)

**`N = 179`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `50.06`

## Goal

Produce a human-readable run report (markdown + HTML) after each orchestrated run: per-product
result, findings summary, and a service-coverage rollup derived from each `demo.json.services`.

## Dependencies

* `50.05` orchestrator.

## Scope

* `harness/report.ts`: write `artifacts/report.md` + `artifacts/report.html` with product results,
  durations, findings counts, and links to trace/video artifacts.
* Coverage rollup: union of `services` across run products vs
  [`service-coverage-matrix.md`](../../../demo-projects/service-coverage-matrix.md); show covered/
  uncovered (informational here; enforced in 56.02).
* `make e2e-report` opens the last report.

## Explicitly out of scope

* Failing the run on missing coverage (56.02).

## Implementation tasks

1. Implement report generation from the orchestrator result object.
2. Implement coverage rollup table.
3. Add `make e2e-report`.

## Files to create

* `tests/e2e/harness/report.ts`

## Files to modify

* `Makefile` (`e2e-report`); `docs/implementation/progress.md` (mark 50.06).

## Unit / integration tests

* Report renders from a fixture result with mixed pass/degraded/fail.
* Coverage rollup marks a service uncovered when no product lists it.

## Demo scenario

Report + coverage generated after the 50.07 self-test run.

## Manual verification

```bash
make test-platform-e2e PROJECTS=50 && make e2e-report
```

## Acceptance criteria

* Report + coverage rollup produced with artifact links after every run.

## Expected Git commit

```text
test(50.06): add e2e run report and coverage rollup
```
