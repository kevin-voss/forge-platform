# Step 54.01: Multi-service scaffold + Postgres

## Status

Not started

## Epic

`54-demo-orderpipe` → [`../../epics/54-demo-orderpipe.md`](../../epics/54-demo-orderpipe.md)

## Global sequence (N)

**`N = 199`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `54.01`

## Goal

Create the OrderPipe multi-service product — storefront SPA + `order-api` (Go) + `fulfillment`
(Python) + `notify` (Elixir) — with orders schema in managed Postgres, all deployed to Ready.

## Dependencies

* Epic 50; Build, Control, Gateway, managed Postgres.

## Scope

* `demos/54-orderpipe/`: four services + Dockerfiles + build/Application/Route docs; storefront on
  `shop.orderpipe.localhost`, api on `api.orderpipe.localhost`; `fulfillment`/`notify` internal.
* `orders`/`order_items`/`saga_events` schema + migrations; place-order stub (synchronous until
  54.04/54.05).
* `run.sh`, `seed.sh` (catalog), `demo.json`.

## Explicitly out of scope

* Discovery, network policy, events, workflow (54.02–54.05).

## Implementation tasks

1. Scaffold four services + manifests.
2. DB schema + migrations + place-order stub.
3. `run.sh`/`seed.sh`/`demo.json`.

## Files to create

* `demos/54-orderpipe/**`

## Files to modify

* `Makefile` (demo 54 wiring if needed); `progress.md`.

## Unit / integration tests

* Orders repository test; each service health test.

## Demo scenario

Deploy → storefront loads → place order creates an `orders` row.

## Manual verification

```bash
make demo DEMO=54
curl -fsS -H 'Host: shop.orderpipe.localhost' http://127.0.0.1:4000/
```

## Acceptance criteria

* All four services deploy to Ready; storefront + api serve; order rows persist.

## Expected Git commit

```text
test(54.01): scaffold OrderPipe multi-service product
```
