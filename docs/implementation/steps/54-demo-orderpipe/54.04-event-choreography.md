# Step 54.04: Event choreography

## Status

Complete

## Epic

`54-demo-orderpipe` → [`../../epics/54-demo-orderpipe.md`](../../epics/54-demo-orderpipe.md)

## Global sequence (N)

**`N = 202`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `54.04`

## Goal

Carry order state between services with Forge Events: services emit and consume `order.*` events so
the order status advances as each stage completes.

## Dependencies

* `54.03`; Events (epic 11).

## Scope

* Define `order.placed/validated/charged/fulfilled/notified` events + schemas.
* order-api emits on placement; fulfillment/notify react and emit their own; status column advances;
  `saga_events` mirrors the audit trail.

## Explicitly out of scope

* Retry/compensation orchestration (54.05 — Workflows).

## Implementation tasks

1. Define event schemas.
2. Emit/consume across services; advance status.
3. Persist the audit mirror.

## Files to create

* event schema docs; publish/consume code per service.

## Files to modify

* `demo.json` (`services` += events); `progress.md`.

## Unit / integration tests

* Emitting `order.placed` drives the chain to `notified` (happy path, no failures).

## Demo scenario

Place order → status advances placed→validated→charged→fulfilled→notified via events.

## Manual verification

```bash
make demo DEMO=54
# place order; watch status advance through event-driven stages
```

## Acceptance criteria

* Order status advances via events; audit trail consistent with final state.

## Expected Git commit

```text
test(54.04): add OrderPipe event choreography
```
