# Step 54.07: Demo + epic gate

## Status

Complete

## Epic

`54-demo-orderpipe` → [`../../epics/54-demo-orderpipe.md`](../../epics/54-demo-orderpipe.md)

## Global sequence (N)

**`N = 205`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `54.07`

## Goal

Wire OrderPipe into `make demo DEMO=54` and the orchestrator; pass end-to-end headed and headless;
mark epic 54 complete.

## Dependencies

* `54.01`–`54.06`.

## Scope

* Finalize `demo.json`, `run.sh`, `seed.sh`.
* `make demo DEMO=54` and `make test-platform-e2e PROJECTS=04` both pass.
* Record any platform findings (workflow, events, discovery, network).

## Explicitly out of scope

* Aggregate/coverage gate (epic 56).

## Implementation tasks

1. Finalize scripts + `demo.json`.
2. Verify both run paths headed + headless.
3. Update epic + progress status.

## Files to create

* `demos/54-orderpipe/README.md` (runbook) if missing.

## Files to modify

* `docs/implementation/epics/54-demo-orderpipe.md` (Complete); `progress.md`; coverage matrix note.

## Unit / integration tests

* Covered by demo + orchestrator run.

## Demo scenario

`deploy → place orders (happy+declined) → assert saga/policy → findings → teardown` green.

## Manual verification

```bash
make demo DEMO=54
make test-platform-e2e PROJECTS=04
make test-platform-e2e HEADLESS=1 PROJECTS=04
```

## Acceptance criteria

* OrderPipe passes headed + headless via both paths; zero blocker findings; epic 54 Complete.

## Expected Git commit

```text
test(54.07): wire OrderPipe demo and epic 54 gate
```
