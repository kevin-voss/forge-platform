# Step 54.03: Network policy

## Status

Complete

## Epic

`54-demo-orderpipe` → [`../../epics/54-demo-orderpipe.md`](../../epics/54-demo-orderpipe.md)

## Global sequence (N)

**`N = 201`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `54.03`

## Goal

Constrain service-to-service traffic with a Forge NetworkPolicy: allow the pairs the saga needs,
deny the rest, and prove enforcement (a denied pair is blocked; allowed pairs work).

## Dependencies

* `54.02`; Network (epic 22).

## Scope

* `NetworkPolicy orderpipe-mesh`: allow `order-api→fulfillment`, `order-api→notify`; `default: deny`
  (so `fulfillment↔notify` is blocked).
* A debug endpoint to attempt a denied call for the test to observe the block (error/metric/event).

## Explicitly out of scope

* Workflow/event logic (54.04/54.05).

## Implementation tasks

1. Author the NetworkPolicy resource.
2. Add the denied-call debug path.
3. Verify allow works and deny is enforced (deny metric/event observed).

## Files to create

* `NetworkPolicy` resource doc; debug endpoint.

## Files to modify

* `demo.json` (`services` += network); `progress.md`.

## Unit / integration tests

* Allowed pair succeeds; denied pair fails/blocked.

## Demo scenario

order-api→fulfillment works; fulfillment→notify is blocked.

## Manual verification

```bash
make demo DEMO=54
# trigger denied call → blocked; allowed calls succeed
```

## Acceptance criteria

* NetworkPolicy allows required pairs and blocks the denied pair, with enforcement observable.

## Expected Git commit

```text
test(54.03): add OrderPipe network policy and enforcement proof
```
