# Step 54.06: E2E browser spec

## Status

Complete

## Epic

`54-demo-orderpipe` → [`../../epics/54-demo-orderpipe.md`](../../epics/54-demo-orderpipe.md)

## Global sequence (N)

**`N = 204`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `54.06`

## Goal

Drive OrderPipe through the storefront: place an order and watch status advance to `notified`; place
a failing order and watch retry + compensation; prove the network-policy block.

## Dependencies

* `54.01`–`54.05`; harness.

## Scope

* `tests/e2e/projects/04-orderpipe/spec.ts` per
  [`projects/04-orderpipe.md` §6](../../../demo-projects/projects/04-orderpipe.md).
* Product assertions: happy order → `notified`; declined order → `refunded`/`failed` cleanly.
* Platform assertions: discovery resolves peers (no hard-coded DNS), NetworkPolicy blocks denied
  pair, workflow retries + compensates, final DB state matches saga outcome.

## Explicitly out of scope

* Demo/gate wiring (54.07).

## Implementation tasks

1. Author the spec (happy + failure + policy).
2. Add platform assertions.
3. Confirm headed + headless.

## Files to create

* `tests/e2e/projects/04-orderpipe/spec.ts`

## Files to modify

* `demo.json` (`spec`); `progress.md`.

## Unit / integration tests

* The spec is the integration test.

## Demo scenario

Watch a happy order reach `notified` and a declined order compensate; see a denied call blocked.

## Manual verification

```bash
cd tests/e2e && npx playwright test projects/04-orderpipe
HEADLESS=1 npx playwright test projects/04-orderpipe
```

## Acceptance criteria

* Spec passes headed + headless; happy path, compensation, and policy enforcement all proven.

## Expected Git commit

```text
test(54.06): add OrderPipe browser e2e spec
```
