# Step 54.02: Service discovery wiring

## Status

Not started

## Epic

`54-demo-orderpipe` → [`../../epics/54-demo-orderpipe.md`](../../epics/54-demo-orderpipe.md)

## Global sequence (N)

**`N = 200`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `54.02`

## Goal

Make services resolve each other via Forge Discovery (`*.svc.forge`) rather than hard-coded
container DNS, and prove it with a contract check.

## Dependencies

* `54.01`; Discovery (epic 21).

## Scope

* `order-api` calls `fulfillment.svc.forge` / `notify.svc.forge`; endpoints registered + Ready-aware.
* Contract check greps product source for hard-coded peer DNS (compose service names, IPs) → fail
  if found (mirrors capstone `12-contracts.sh`).

## Explicitly out of scope

* Network policy enforcement (54.03).

## Implementation tasks

1. Switch peer addressing to `.svc.forge` names.
2. Confirm endpoint registration + readiness selection.
3. Add the no-hard-coded-DNS contract check.

## Files to create

* discovery contract-check script under `demos/54-orderpipe/`.

## Files to modify

* order-api client config; `demo.json` (`services` += discovery); `progress.md`.

## Unit / integration tests

* Contract check passes (no hard-coded DNS).
* order-api resolves + reaches peers via Discovery.

## Demo scenario

Place order → order-api reaches fulfillment/notify by discovery name.

## Manual verification

```bash
make demo DEMO=54
# confirm peer calls resolve via *.svc.forge; contract check passes
```

## Acceptance criteria

* Services communicate only via Discovery names; contract check green.

## Expected Git commit

```text
test(54.02): wire OrderPipe service discovery
```
