# Step 54.05: Workflow saga + retry/compensation

## Status

Complete

## Epic

`54-demo-orderpipe` → [`../../epics/54-demo-orderpipe.md`](../../epics/54-demo-orderpipe.md)

## Global sequence (N)

**`N = 203`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `54.05`

## Goal

Drive the order lifecycle with a Forge Workflow saga (`validate → charge → fulfill → notify`) that
retries a failing step and compensates on terminal failure, leaving a clean order state.

## Dependencies

* `54.04`; Workflows (epic 16); Secrets (mock PSP key).

## Scope

* `Workflow order-saga` with per-step `retries` and `compensate` actions; charge reads a mock PSP
  key from Secrets; an injectable "declined" toggle forces charge failure.
* Happy path → `notified`; failure path → retries then compensation (refund/cancel) → terminal
  `refunded`/`failed` with no half-fulfilled state.

## Explicitly out of scope

* E2E browser spec (54.06).

## Implementation tasks

1. Author the Workflow saga resource + step handlers.
2. Wire the injectable charge failure.
3. Verify retries + compensation + idempotent side effects.
4. Record any workflow contract gap as a finding.

## Files to create

* `Workflow` resource doc; step handlers.

## Files to modify

* order-api; Secrets provisioning (`run.sh`); `demo.json` (`services` += workflows, secrets);
  `progress.md`.

## Unit / integration tests

* Happy path reaches `notified`.
* Forced charge failure → configured retries → compensation → clean terminal state (idempotent).

## Demo scenario

Normal order completes; a "declined" order retries then compensates cleanly.

## Manual verification

```bash
make demo DEMO=54
# place a normal order (→notified) and a declined order (→retry→compensate)
```

## Acceptance criteria

* Saga performs retries + compensation; final state consistent; no lost/duplicated side effects.

## Expected Git commit

```text
test(54.05): add OrderPipe workflow saga with retry and compensation
```
