# Step 18.06: CLI `forge database *` + demo + gate

## Status

Complete

## Epic

`18-managed-postgresql` → [`../../epics/18-managed-postgresql.md`](../../epics/18-managed-postgresql.md)

## Global sequence (N)

**`N = 125`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md).

Internal id (file/commit): `18.06`

## Goal

Add `forge database` CLI subcommands (create/attach/list/backup/restore/rotate/delete) and deliver `demos/18-managed-database` with a one-command acceptance suite: deploy an app needing Postgres → create → attach → deploy → app runs migrations + writes → backup → restore recovers fixture data. This is the epic acceptance gate.

## Why this step exists

The final DX (`specs.md` §14) includes `forge database create/attach`, and the epic is done only when the full provisioning lifecycle is proven end-to-end with a real product app receiving injected credentials.

## Dependencies

* `18.05` (rotation + deletion protection)
* Epic `03-forge-cli` framework; Runtime/Build/Gateway for deploying the demo app

## Scope

* `forge database create|attach|detach|list|backup|restore|rotate|delete` subcommands
* `demos/18-managed-database/` Compose + a small app requiring Postgres + migrations
* Acceptance script implementing the spec flow with assertions
* Demo README; wire `make demo DEMO=18`
* Assert product receives no hardcoded credentials

## Explicitly out of scope

* New provisioning features (18.01–18.05)
* Capstone (epic 19)

## Architecture

```text
demos/18-managed-database/
├── compose.yaml
├── app/                # tiny app: reads DATABASE_URL, migrates, writes/reads
├── run.sh
├── acceptance.sh
└── README.md

forge database create main --project proj-a
forge database attach main --app backend --env DATABASE_URL
forge deploy → app gets injected URL → migrates → writes
forge database backup ... ; forge database restore ...
```

## Components

| Component | Purpose |
|---|---|
| `forge database` CLI | Thin client over Control managed-db APIs |
| Demo app | Uses injected `DATABASE_URL`, runs a migration + write/read |
| `acceptance.sh` | Falsifiable end-to-end assertions |
| README | Scenario explanation |

## Contracts

CLI uses only documented managed-db endpoints. Demo app relies on injected env var (no hardcoded creds).

## Data model

None new (uses managed-db resources).

## Configuration

```text
DEMO=18
FORGE_DB_PROVISIONER=local
```

## Security considerations

* Demo asserts no plaintext credentials in app config/logs
* No secrets committed

## Observability

* `run.sh` prints per-step assertions; on failure dumps Control + app logs (URL masked)

## Failure handling

* Wait-for-ready gates; any failed assertion → non-zero exit; teardown always runs

## Implementation tasks

1. Implement `forge database` subcommands + output/exit codes.
2. Build the demo app (reads `DATABASE_URL`, migrates, writes/reads a fixture row).
3. Implement `acceptance.sh` (create → attach → deploy → migrate → write → backup → restore → verify fixture).
4. Assert no hardcoded credentials in the app.
5. Write `run.sh` + README; wire `make demo DEMO=18`.
6. Update roadmap/README status.

## Files to create

* `tools/forge-cli/cmd/database.go` (or equivalent)
* `demos/18-managed-database/compose.yaml`
* `demos/18-managed-database/app/` (minimal DB app + migration)
* `demos/18-managed-database/run.sh`
* `demos/18-managed-database/acceptance.sh`
* `demos/18-managed-database/README.md`

## Files to modify

* `tools/forge-cli` command registration
* `Makefile` (demo routing if needed)
* `docs/implementation/progress.md` (epic 18 → Complete after gate)
* `docs/implementation/epics/18-managed-postgresql.md`
* `docs/implementation/roadmap.md` / `README.md`

## Unit tests

* CLI arg parsing + output formatting + exit codes
* Demo app migration/write/read against a URL

## Integration tests

* `run.sh` green locally end-to-end

## Contract tests

* Pre-step OpenAPI validity; CLI uses only documented endpoints

## Demo scenario

```text
1. forge database create main → isolated Postgres provisioned
2. forge database attach main --app backend
3. forge deploy → app receives DATABASE_URL (from Secrets), no hardcoded creds
4. app runs migrations and writes fixture data
5. forge database backup → archive stored
6. forge database restore → fixture data recovered and verified
```

## Manual verification

```bash
make demo DEMO=18
echo "exit=$?"   # 0 on success
```

## Acceptance criteria

* `make demo DEMO=18` exits `0`
* `forge database` subcommands drive the full lifecycle against Control
* The demo app receives a working `DATABASE_URL` via injection with NO hardcoded credentials
* Databases are isolated; backup + restore recovers known fixture data
* Rotated credentials invalidate old ones (asserted or referenced from 18.05)
* Demo README documents the flow
* Epic 18 marked Complete in `progress.md` only after this gate passes

## Definition of done

* Acceptance criteria pass
* Docs updated
* `progress.md` updated
* One focused Git commit

## Expected Git commit

```text
feat(18.06): add forge database CLI and managed-database demo gate
```
