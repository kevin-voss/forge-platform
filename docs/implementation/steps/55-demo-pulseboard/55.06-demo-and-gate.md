# Step 55.06: Demo + epic gate

## Status

Complete

## Epic

`55-demo-pulseboard` → [`../../epics/55-demo-pulseboard.md`](../../epics/55-demo-pulseboard.md)

## Global sequence (N)

**`N = 211`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `55.06`

## Goal

Wire PulseBoard into `make demo DEMO=55` and the orchestrator; pass end-to-end headed and headless;
mark epic 55 complete.

## Dependencies

* `55.01`–`55.05`.

## Scope

* Finalize `demo.json`, `run.sh`, load generator, NodePool.
* `make demo DEMO=55` and `make test-platform-e2e PROJECTS=05` both pass.
* Record any platform findings (metric freshness, flapping, node latency, replica consistency).

## Explicitly out of scope

* Aggregate/coverage gate (epic 56).

## Implementation tasks

1. Finalize scripts + `demo.json`.
2. Verify both run paths headed + headless.
3. Update epic + progress status.

## Files to create

* `demos/55-pulseboard/README.md` (runbook) if missing.

## Files to modify

* `docs/implementation/epics/55-demo-pulseboard.md` (Complete); `progress.md`; coverage matrix note.

## Unit / integration tests

* Covered by demo + orchestrator run.

## Demo scenario

`deploy → load → scale up (UI+Grafana) → optional node → scale down → findings → teardown` green.

## Manual verification

```bash
make demo DEMO=55
make test-platform-e2e PROJECTS=05
make test-platform-e2e HEADLESS=1 PROJECTS=05
```

## Acceptance criteria

* PulseBoard passes headed + headless via both paths; zero blocker findings; epic 55 Complete.

## Expected Git commit

```text
test(55.06): wire PulseBoard demo and epic 55 gate
```
