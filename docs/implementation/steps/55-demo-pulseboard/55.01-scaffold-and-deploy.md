# Step 55.01: Product scaffold + baseline deploy

## Status

Not started

## Epic

`55-demo-pulseboard` → [`../../epics/55-demo-pulseboard.md`](../../epics/55-demo-pulseboard.md)

## Global sequence (N)

**`N = 206`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `55.01`

## Goal

Create the PulseBoard product (Go API + live dashboard SPA, stateless) and deploy it to Ready,
reachable through the Gateway at `board.pulseboard.localhost` / `api.pulseboard.localhost`.

## Dependencies

* Epic 50; Build, Control, Gateway.

## Scope

* `demos/55-pulseboard/`: `pulseboard-api` (counter + `/stats` + `/health/ready`, `resources` set
  modestly so scaling is observable), dashboard SPA, Dockerfiles, build + Application/Route docs.
* `run.sh`, `demo.json` (no database — intentionally stateless).

## Explicitly out of scope

* Autoscaling, node scaling, observe surfacing (55.02–55.04).

## Implementation tasks

1. Scaffold API + dashboard + Dockerfiles + manifests.
2. Baseline deploy (min 1 replica).
3. `run.sh`/`demo.json`.

## Files to create

* `demos/55-pulseboard/**`

## Files to modify

* `Makefile` (demo 55 wiring if needed); `progress.md`.

## Unit / integration tests

* `/stats` + health handler test.

## Demo scenario

Deploy → dashboard loads showing replicas: 1.

## Manual verification

```bash
make demo DEMO=55
curl -fsS -H 'Host: api.pulseboard.localhost' http://127.0.0.1:4000/health/ready
```

## Acceptance criteria

* PulseBoard deploys to Ready; dashboard + api serve; baseline 1 replica.

## Expected Git commit

```text
test(55.01): scaffold PulseBoard product and baseline deploy
```
