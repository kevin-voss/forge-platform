# Step 55.05: E2E browser spec

## Status

Complete

## Epic

`55-demo-pulseboard` → [`../../epics/55-demo-pulseboard.md`](../../epics/55-demo-pulseboard.md)

## Global sequence (N)

**`N = 210`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `55.05`

## Goal

Drive PulseBoard through the browser: watch replicas scale up under load in the dashboard (matching
Grafana), optionally trigger a node add, then watch scale-down after load stops.

## Dependencies

* `55.01`–`55.04`; harness.

## Scope

* `tests/e2e/projects/05-pulseboard/spec.ts` per
  [`projects/05-pulseboard.md` §6](../../../demo-projects/projects/05-pulseboard.md).
* Product assertions: dashboard replica count rises under load and falls after, within bounds.
* Platform assertions: Gateway RPS → autoscaler status, Control actuates replicas, node add on
  unschedulability + drain, dashboard vs Grafana consistency.
* Assert direction + bounds only (see harness §9); generous stabilization timeouts.

## Explicitly out of scope

* Demo/gate wiring (55.06).

## Implementation tasks

1. Author the spec with the harness load generator.
2. Add platform assertions.
3. Confirm headed + headless (node leg optional/thresholded for CI speed).

## Files to create

* `tests/e2e/projects/05-pulseboard/spec.ts`

## Files to modify

* `demo.json` (`spec`); `progress.md`.

## Unit / integration tests

* The spec is the integration test.

## Demo scenario

Watch replicas climb under load in the browser + Grafana, then fall after load stops.

## Manual verification

```bash
cd tests/e2e && npx playwright test projects/05-pulseboard
HEADLESS=1 npx playwright test projects/05-pulseboard
```

## Acceptance criteria

* Spec passes headed + headless; scale up/down (and optional node leg) visibly proven within bounds.

## Expected Git commit

```text
test(55.05): add PulseBoard browser e2e spec
```
