# Step 55.02: HTTP request-rate autoscaling + load generator

## Status

Not started

## Epic

`55-demo-pulseboard` → [`../../epics/55-demo-pulseboard.md`](../../epics/55-demo-pulseboard.md)

## Global sequence (N)

**`N = 207`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `55.02`

## Goal

Scale the API by HTTP request rate: a load generator raises RPS, replicas grow within bounds, and
after load stops replicas scale back down.

## Dependencies

* `55.01`; Autoscaler (epic 24, httpRequests); Gateway metric source.

## Scope

* `ScalingPolicy { type: httpRequests, targetRequestsPerSecond }` on the API
  (`minReplicas: 1, maxReplicas: 10`).
* A harness-driven load generator against `api.pulseboard.localhost`.
* Assert replica direction + bounds (up under load, down after), never exact counts/timing.

## Explicitly out of scope

* Node scaling (55.03); dashboard surfacing (55.04).

## Implementation tasks

1. Author the ScalingPolicy.
2. Add the load generator (start/stop) to the harness/`run.sh`.
3. Verify scale up then down within bounds.

## Files to create

* ScalingPolicy resource doc; load generator.

## Files to modify

* `demo.json` (`services` += autoscaler); `progress.md`.

## Unit / integration tests

* Policy status reflects RPS; replicas within `[min,max]`.

## Demo scenario

Start load → replicas climb; stop load → replicas fall.

## Manual verification

```bash
make demo DEMO=55
# start load; watch replicas rise; stop; watch fall
```

## Acceptance criteria

* API replicas scale up under load and down after, within bounds.

## Expected Git commit

```text
test(55.02): add HTTP request-rate autoscaling to PulseBoard
```
