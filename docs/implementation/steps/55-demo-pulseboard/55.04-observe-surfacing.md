# Step 55.04: Observe surfacing

## Status

Complete

## Epic

`55-demo-pulseboard` → [`../../epics/55-demo-pulseboard.md`](../../epics/55-demo-pulseboard.md)

## Global sequence (N)

**`N = 209`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `55.04`

## Goal

Make autoscaling watchable in the product: the dashboard reads replica count, RPS, and p95 from
Forge Observe and the values match Grafana.

## Dependencies

* `55.03`; Observe (epic 12); Grafana.

## Scope

* API emits OTEL metrics/traces/logs; dashboard `/stats` queries Observe for replica count / RPS /
  p95 and renders them live.
* Cross-check the dashboard's replica count against Grafana/Observe for consistency.

## Explicitly out of scope

* E2E browser spec (55.05).

## Implementation tasks

1. Emit + query the metrics via Observe.
2. Render them live in the dashboard.
3. Verify dashboard vs Grafana consistency.

## Files to create

* Observe query client; dashboard panels.

## Files to modify

* API telemetry; `demo.json` (`services` += observe); `progress.md`.

## Unit / integration tests

* `/stats` returns Observe-sourced values; matches a direct Observe/Grafana query.

## Demo scenario

Dashboard shows live replica count / RPS / p95 matching Grafana.

## Manual verification

```bash
make demo DEMO=55
# compare dashboard replica count to Grafana (http://127.0.0.1:3000)
```

## Acceptance criteria

* Dashboard surfaces Observe metrics that match Grafana within tolerance.

## Expected Git commit

```text
test(55.04): surface Observe metrics in PulseBoard dashboard
```
