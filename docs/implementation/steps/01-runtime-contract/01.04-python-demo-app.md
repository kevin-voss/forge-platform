# Step 01.04: Python demo application

## Status

Complete

## Epic

`01-runtime-contract` → [`../../epics/01-runtime-contract.md`](../../epics/01-runtime-contract.md)

## Global sequence (N)

**`N = 4`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md).

Internal id (file/commit): `01.04`

## Goal

Add a Python workload that implements the same runtime contract as the Go reference, published on host port **4204**, and extend demo 01 to validate it.

## Why this step exists

Proves the contract is language-agnostic beyond Go, using a common scripting/runtime stack for AI-adjacent services later.

## Dependencies

* `01.03` — demo scaffold + Go reference patterns to mirror

## Scope

* Python app under `demos/01-container-runtime/apps/python/`
* Dockerfile; bind `PORT`; `/health/live`, `/health/ready`, `/`; JSON logs; SIGTERM
* Compose service on host port **4204**
* Extend `run.sh` to build/start/validate Python (Go may remain in the suite)
* Framework choice left to implementer (stdlib `http.server`, FastAPI, Flask, etc.) — keep dependencies minimal

## Explicitly out of scope

* Kotlin / Rust / Elixir
* Platform Python SDK (`packages/python-sdk`)
* OTEL exporters as required

## Architecture

Same Compose project as `01.03`; add one service:

```text
demo-python-api :4204 → contract-validator
```

## Components

| Component | Host port |
|---|---:|
| `demo-python-api` | 4204 |

## Contracts

Identical OpenAPI surface; identity `language` = `"python"`, `service` = `"demo-python-api"`.

## Data model

None.

## Configuration

Same env subset as Go (`PORT`, `FORGE_SERVICE_NAME`, `FORGE_SERVICE_VERSION`, `FORGE_LOG_LEVEL`, optional `FORGE_ENV`).

## Security considerations

* Non-root container user when practical
* Pin base image tag

## Observability

Structured JSON logs to stdout per log schema.

## Failure handling

Same as Go: invalid `PORT` fails startup; SIGTERM exits within grace.

## Implementation tasks

1. Implement Python HTTP service + Dockerfile.
2. Add Compose service + port 4204.
3. Extend `demos/01-container-runtime/run.sh` / README for Python.
4. Run shared validator against Python.
5. Keep Go checks green (regression).

## Files to create

* `demos/01-container-runtime/apps/python/**`

## Files to modify

* `demos/01-container-runtime/compose.yaml`
* `demos/01-container-runtime/run.sh`
* `demos/01-container-runtime/README.md`
* `docs/implementation/progress.md`

## Unit tests

* Lightweight tests for handlers / JSON identity (pytest or unittest)

## Integration tests

* Container build + validator
* Graceful shutdown via `docker stop`

## Contract tests

* `contract-validator` with `--expect-language python`

## Demo scenario

```bash
make demo DEMO=01
```

Demo must validate at least Go + Python after this step.

## Manual verification

```bash
curl -sf http://127.0.0.1:4204/health/ready
curl -sf http://127.0.0.1:4204/ | grep -q '"language":"python"'
make demo DEMO=01
```

## Acceptance criteria

* Python image builds and passes the shared validator
* Endpoints and shutdown match the contract
* Demo 01 still passes (Go + Python)
* No platform SDK usage

## Definition of done

* Acceptance criteria pass
* Docs updated
* `progress.md` updated
* One focused Git commit

## Expected Git commit

```text
feat(01.04): add Python runtime-contract demo app
```
