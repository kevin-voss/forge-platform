# Step 01.07: Elixir demo and full five-language suite

## Status

Not started

## Epic

`01-runtime-contract` → [`../../epics/01-runtime-contract.md`](../../epics/01-runtime-contract.md)

## Global sequence (N)

**`N = 7`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md).

Internal id (file/commit): `01.07`

## Goal

Complete the fifth language (Elixir) on host port **4205**, then harden demo 01 so all five containers start via Compose, pass the shared validator, and satisfy every Step 01 acceptance criterion from `specs.md`.

## Why this step exists

Specs require five languages and a passing `demos/01-container-runtime`. Elixir closes the polyglot set (also foreshadowing Workflows) and this step is the epic-level acceptance gate.

## Dependencies

* `01.03`–`01.06` — Go, Python, Kotlin, and Rust demos exist
* `01.02` — shared validator

## Scope

* Elixir/Phoenix-or-Bandit/Plug minimal app under `demos/01-container-runtime/apps/elixir/`
* Dockerfile; `PORT`; health + `/`; structured JSON logs; SIGTERM (`Application.stop` / OTP shutdown)
* Compose service on **4205**
* Finalize `run.sh` to start **all five** services and validate each
* Confirm tests from `specs.md` Step 01:
  * every image builds
  * every container starts
  * health endpoints succeed
  * shutdown without forced termination
  * required env vars respected
  * logs contain required fields
* Polish demo README + cross-links from `docs/contracts/runtime-contract.md`
* Optional: CI job or Make target that runs demo 01 (only if foundation CI pattern already supports demos)

## Explicitly out of scope

* Platform services (`forge-workflows`, etc.)
* SDK packages
* Requiring `/metrics` or OTEL for demos
* Epic 02 Control plane work

## Architecture

```text
Docker Compose (demo 01)
├── demo-go-api      :4201
├── demo-kotlin-api  :4202
├── demo-rust-api    :4203
├── demo-python-api  :4204
└── demo-elixir-api  :4205
         │
         ▼
tools/contract-validator (×5)
```

## Components

| Component | Host port | Language |
|---|---:|---|
| `demo-go-api` | 4201 | Go |
| `demo-kotlin-api` | 4202 | Kotlin |
| `demo-rust-api` | 4203 | Rust |
| `demo-python-api` | 4204 | Python |
| `demo-elixir-api` | 4205 | Elixir |

## Contracts

Elixir identity: `service` = `"demo-elixir-api"`, `language` = `"elixir"`.

Epic-level rule restated: **no application contains platform-specific SDK dependencies**.

## Data model

None.

## Configuration

All five services use the same env contract; demo Compose should inject distinct `FORGE_SERVICE_NAME` values.

## Security considerations

* Same as prior demos; document any BEAM-specific port/cookie non-requirements (no distributed Erlang required)

## Observability

JSON logs; demo README notes where to `docker compose logs`.

## Failure handling

* Suite fails if any language fails validator or shutdown check
* Partial startup must not report overall success

## Implementation tasks

1. Implement Elixir demo app + Dockerfile.
2. Add Compose service on 4205.
3. Update `run.sh` to orchestrate all five build/up/validate/shutdown checks.
4. Expand README with architecture diagram and curl examples for each port.
5. Run full acceptance battery; fix cross-cutting gaps (env naming, log fields) without redesigning the contract.
6. Mark epic complete in progress/epic docs after green demo.

## Files to create

* `demos/01-container-runtime/apps/elixir/**`

## Files to modify

* `demos/01-container-runtime/compose.yaml`
* `demos/01-container-runtime/run.sh`
* `demos/01-container-runtime/README.md`
* `docs/contracts/runtime-contract.md` (final “Demo 01” pointer)
* `docs/implementation/epics/01-runtime-contract.md`
* `docs/implementation/progress.md`
* Optionally `.github/workflows/*` if a demo job is in scope and low-risk

## Unit tests

* Elixir endpoint tests (ExUnit) for health/identity

## Integration tests

* Full Compose stack for demo 01
* Per-service graceful `docker stop`

## Contract tests

* Validator against all five base URLs

## Demo scenario

```bash
make setup   # if needed
make demo DEMO=01
```

Success: all five images build, health OK, validator OK, clean shutdown, summary printed.

## Manual verification

```bash
make demo DEMO=01
for p in 4201 4202 4203 4204 4205; do curl -sf http://127.0.0.1:$p/health/ready; done
```

## Acceptance criteria

* All five languages implement the same contract
* Shared test runner validates all applications
* Runtime contract remains documented and consistent
* No platform SDK dependencies in any demo app
* `make demo DEMO=01` passes end-to-end
* Specs Step 01 test list is covered

## Definition of done

* Acceptance criteria pass
* Docs updated
* `progress.md` updated (epic → Complete when this lands)
* One focused Git commit

## Expected Git commit

```text
feat(01.07): add Elixir demo and complete runtime-contract suite
```
