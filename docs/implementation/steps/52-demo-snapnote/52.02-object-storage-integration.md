# Step 52.02: Object storage integration

## Status

Not started

## Epic

`52-demo-snapnote` → [`../../epics/52-demo-snapnote.md`](../../epics/52-demo-snapnote.md)

## Global sequence (N)

**`N = 188`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `52.02`

## Goal

Attach files to notes by uploading to Forge Storage via presigned URLs and retrieving them back.

## Dependencies

* `52.01`; Storage (epic 13).

## Scope

* `dependencies.storage { type: object, bucket: snapnote-attachments }`.
* API issues presigned PUT for upload and presigned/streamed GET for download; browser uploads
  directly to storage; attachment metadata rows created with `object_key`, `status=pending`.

## Explicitly out of scope

* Async processing/thumbnails (52.03) — status stays `pending` here.

## Implementation tasks

1. Declare the storage dependency; wire the storage client.
2. Implement presign upload + retrieval endpoints.
3. Store attachment metadata.

## Files to create

* storage client + handlers under `demos/52-snapnote/`.

## Files to modify

* Application resource doc; SPA upload UI; `progress.md`.

## Unit / integration tests

* Presign + PUT + GET round-trip against Storage (integration).

## Demo scenario

Upload a file → object exists in Storage → downloadable via the app.

## Manual verification

```bash
make demo DEMO=52
# upload an attachment; confirm object exists and is retrievable
```

## Acceptance criteria

* Files upload to and download from Forge Storage; metadata recorded; object keys correct.

## Expected Git commit

```text
test(52.02): add object storage attachments to SnapNote
```
