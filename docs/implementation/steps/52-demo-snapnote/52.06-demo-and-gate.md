# Step 52.06: Demo + epic gate

## Status

Not started

## Epic

`52-demo-snapnote` → [`../../epics/52-demo-snapnote.md`](../../epics/52-demo-snapnote.md)

## Global sequence (N)

**`N = 192`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `52.06`

## Goal

Wire SnapNote into `make demo DEMO=52` and the orchestrator; pass end-to-end headed and headless;
mark epic 52 complete.

## Dependencies

* `52.01`–`52.05`.

## Scope

* Finalize `demo.json`, `run.sh`, `seed.sh`, burst helper.
* `make demo DEMO=52` and `make test-platform-e2e PROJECTS=02` both pass.
* Record any platform findings (queue semantics, autoscaler metric freshness, etc.).

## Explicitly out of scope

* Aggregate/coverage gate (epic 56).

## Implementation tasks

1. Finalize scripts + `demo.json`.
2. Verify both run paths headed + headless.
3. Update epic + progress status.

## Files to create

* `demos/52-snapnote/README.md` (runbook) if missing.

## Files to modify

* `docs/implementation/epics/52-demo-snapnote.md` (Complete); `progress.md`; coverage matrix note.

## Unit / integration tests

* Covered by demo + orchestrator run.

## Demo scenario

`deploy → seed → upload/thumbnail → burst/scale → assert → findings → teardown` green.

## Manual verification

```bash
make demo DEMO=52
make test-platform-e2e PROJECTS=02
make test-platform-e2e HEADLESS=1 PROJECTS=02
```

## Acceptance criteria

* SnapNote passes headed + headless via both paths; zero blocker findings; epic 52 Complete.

## Expected Git commit

```text
test(52.06): wire SnapNote demo and epic 52 gate
```
