# Step 52.01: Product scaffold + notes CRUD + Postgres

## Status

Complete

## Epic

`52-demo-snapnote` → [`../../epics/52-demo-snapnote.md`](../../epics/52-demo-snapnote.md)

## Global sequence (N)

**`N = 187`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `52.01`

## Goal

Create the SnapNote product (Go API + SPA) with notes CRUD backed by managed Postgres, deployed and
reachable through the Gateway.

## Dependencies

* Epic 50; Build, Control, Gateway, managed Postgres.

## Scope

* `demos/52-snapnote/`: `snapnote-api` (notes CRUD + attachments schema stub), SPA, Dockerfiles,
  build + Application/Route docs, `dependencies.database`, migrations for `notes`/`attachments`.
* `run.sh`, `seed.sh`, `demo.json` (hosts `app./api.snapnote.localhost`).

## Explicitly out of scope

* Storage, events, autoscaling (52.02–52.04).

## Implementation tasks

1. Scaffold API + SPA + Dockerfiles + manifests.
2. Add DB dependency + migrations + notes CRUD.
3. `run.sh`/`seed.sh`/`demo.json`.

## Files to create

* `demos/52-snapnote/**`

## Files to modify

* `Makefile` (demo 52 wiring if needed); `progress.md`.

## Unit / integration tests

* Notes repository CRUD test.

## Demo scenario

Deploy → create a note through the SPA → persists.

## Manual verification

```bash
make demo DEMO=52
curl -fsS -H 'Host: api.snapnote.localhost' http://127.0.0.1:4000/health/ready
```

## Acceptance criteria

* SnapNote deploys to Ready; notes CRUD persists in managed Postgres; both hosts serve.

## Expected Git commit

```text
test(52.01): scaffold SnapNote product with notes and Postgres
```
