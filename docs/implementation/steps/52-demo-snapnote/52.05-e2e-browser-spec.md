# Step 52.05: E2E browser spec

## Status

Not started

## Epic

`52-demo-snapnote` → [`../../epics/52-demo-snapnote.md`](../../epics/52-demo-snapnote.md)

## Global sequence (N)

**`N = 191`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `52.05`

## Goal

Drive SnapNote through the browser: create a note, attach a file, watch the thumbnail appear
asynchronously, then burst-upload to watch worker replicas scale up and drain.

## Dependencies

* `52.01`–`52.04`; harness.

## Scope

* `tests/e2e/projects/02-snapnote/spec.ts` per
  [`projects/02-snapnote.md` §6](../../../demo-projects/projects/02-snapnote.md).
* Product assertions: upload → "processing" → thumbnail visible; burst → workers indicator rises →
  all `ready`.
* Platform assertions: exactly-once (worker restart mid-burst), ScalingPolicy status tracks depth +
  bounds respected, thumbnail retrievable, (optional) node scale-up/down.

## Explicitly out of scope

* Demo/gate wiring (52.06).

## Implementation tasks

1. Author the spec (single upload + burst).
2. Add platform assertions incl. restart-mid-burst idempotency.
3. Confirm headed + headless with generous async timeouts.

## Files to create

* `tests/e2e/projects/02-snapnote/spec.ts`

## Files to modify

* `demo.json` (`spec`); `progress.md`.

## Unit / integration tests

* The spec is the integration test.

## Demo scenario

Watch a thumbnail appear after upload, then watch replicas scale under a burst.

## Manual verification

```bash
cd tests/e2e && npx playwright test projects/02-snapnote
HEADLESS=1 npx playwright test projects/02-snapnote
```

## Acceptance criteria

* Spec passes headed and headless; async processing + autoscaling visibly proven.

## Expected Git commit

```text
test(52.05): add SnapNote browser e2e spec
```
