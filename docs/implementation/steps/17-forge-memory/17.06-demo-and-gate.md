# Step 17.06: Demo `17-agent-memory` + gate

## Status

Not started

## Epic

`17-forge-memory` → [`../../epics/17-forge-memory.md`](../../epics/17-forge-memory.md)

## Global sequence (N)

**`N = 119`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md).

Internal id (file/commit): `17.06`

## Goal

Deliver `demos/17-agent-memory` and a one-command acceptance suite: store historical deployment incidents in memory, create a new failure with similar symptoms, and show the agent retrieving related incidents and citing them in its diagnosis — the epic acceptance gate.

## Why this step exists

The epic is done only when semantic retrieval demonstrably improves an agent diagnosis end-to-end, with correct NN results, project isolation, and restart durability (`specs.md` Step 17 acceptance).

## Dependencies

* `17.05` (Models embed + Agents memory tools)
* Agents/Models in fake mode for deterministic CI

## Scope

* `demos/17-agent-memory/` Compose wiring memory + models(fake) + agents(fake)
* Seed historical incidents (fixtures) into a project collection
* Acceptance script: store incidents → new similar failure → agent retrieves + cites → assert
* Restart-durability + project-isolation assertions
* Demo README; wire `make demo DEMO=17`

## Explicitly out of scope

* New memory features (17.01–17.05)
* HNSW; capstone integration (epic 19)

## Architecture

```text
demos/17-agent-memory/
├── compose.yaml         # forge-memory + forge-models(fake) + forge-agents(fake)
├── fixtures/incidents.json
├── run.sh
├── acceptance.sh
└── README.md
```

## Components

| Component | Purpose |
|---|---|
| Compose | memory + models(fake) + agents(fake) |
| incident fixtures | Historical incidents to seed |
| `acceptance.sh` | Falsifiable assertions |
| README | Scenario explanation |

## Contracts

Consumes memory + models + agents contracts. No new contracts.

## Data model

None (durable volume for memory persistence within the demo).

## Configuration

```text
DEMO=17
FORGE_MODELS_BACKEND=fake
FORGE_AGENTS_TOOLS_MODE=fake
```

## Security considerations

* Project isolation asserted in the demo
* No secrets committed

## Observability

* `run.sh` prints per-assertion pass/fail + retrieved records
* On failure dumps memory query results + agent run steps

## Failure handling

* Wait-for-ready before seeding
* Any failed assertion → non-zero exit; teardown always runs

## Implementation tasks

1. Create Compose + incident fixtures.
2. Implement `acceptance.sh`:
   - seed historical incidents (embedded) into `proj-a/incidents`
   - assert NN query for a new similar symptom returns the expected incident (correct ordering)
   - run an agent that uses `memory.search` and assert its output cites the retrieved incident id
   - assert project-B cannot see project-A incidents
   - restart `forge-memory` → assert vectors persist + query still returns same result
3. Write `run.sh` + README.
4. Wire `make demo DEMO=17`.
5. Update roadmap/README status + benchmark note.

## Files to create

* `demos/17-agent-memory/compose.yaml`
* `demos/17-agent-memory/fixtures/incidents.json`
* `demos/17-agent-memory/run.sh`
* `demos/17-agent-memory/acceptance.sh`
* `demos/17-agent-memory/README.md`

## Files to modify

* `Makefile` (demo routing if needed)
* `docs/implementation/progress.md` (epic 17 → Complete after gate)
* `docs/implementation/epics/17-forge-memory.md`
* `docs/implementation/roadmap.md` / `README.md`

## Unit tests

* Acceptance assertion helpers (optional)

## Integration tests

* `run.sh` green locally end-to-end including restart + isolation

## Contract tests

* Pre-step OpenAPI validity; demo uses only documented endpoints

## Demo scenario

```text
1. seed historical incidents into proj-a/incidents (embedded via Models fake)
2. create a new failure with similar symptoms
3. NN query returns the most similar historical incident(s)
4. agent uses memory.search and cites the retrieved incident in its diagnosis
5. proj-b sees none of proj-a's incidents (isolation)
6. restart forge-memory → vectors persist; query returns identical results
```

## Manual verification

```bash
make demo DEMO=17
echo "exit=$?"   # 0 on success
```

## Acceptance criteria

* `make demo DEMO=17` exits `0`
* Nearest-neighbor results are correct for the fixture incidents
* An agent retrieves related incidents and cites them in its diagnosis
* Project data is isolated (project B cannot see project A memory)
* Persisted vectors survive a restart with identical query results
* A performance benchmark is documented
* Demo README documents the flow; deterministic in CI
* Epic 17 marked Complete in `progress.md` only after this gate passes

## Definition of done

* Acceptance criteria pass
* Docs updated
* `progress.md` updated
* One focused Git commit

## Expected Git commit

```text
feat(17.06): add agent-memory demo and epic acceptance gate
```
