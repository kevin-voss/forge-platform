# Step 56.03: Findings consolidation + triage

## Status

Complete

## Epic

`56-platform-e2e-gate` → [`../../epics/56-platform-e2e-gate.md`](../../epics/56-platform-e2e-gate.md)

## Global sequence (N)

**`N = 214`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `56.03`

## Goal

After a full run, consolidate all recorded platform findings into a single ranked, deduped, and
grouped [`PLATFORM_FINDINGS.md`](../../../demo-projects/PLATFORM_FINDINGS.md) — the track's primary
deliverable for platform bugs.

## Dependencies

* `56.01`; findings collector (50.04).

## Scope

* Dedupe across products/runs (by `service+title`); rank by severity; group by service and by demo.
* Refresh the summary + by-service + by-demo tables at the top of `PLATFORM_FINDINGS.md`.
* Emit a triage list ("blocker findings first, service owner, suspected component") into the report.
* Verify every finding meets the template's evidence requirement; flag evidence-less entries.

## Explicitly out of scope

* Fixing findings (handed off as separate platform work).

## Implementation tasks

1. Implement consolidation + ranking + grouping from `findings.json`.
2. Regenerate the summary tables.
3. Produce the triage list in the report.

## Files to modify

* `tests/e2e/harness/findings.ts` (consolidation mode); `PLATFORM_FINDINGS.md`; `progress.md`.

## Unit / integration tests

* Consolidation dedupes duplicates and orders by severity; tables reflect counts.

## Demo scenario

After a full run, `PLATFORM_FINDINGS.md` shows a single ranked, grouped list with accurate totals.

## Manual verification

```bash
make test-platform-e2e
# inspect docs/demo-projects/PLATFORM_FINDINGS.md summary + ordering
```

## Acceptance criteria

* Findings are consolidated, deduped, ranked, grouped, and evidence-checked; tables accurate.

## Expected Git commit

```text
test(56.03): consolidate and triage platform findings
```
