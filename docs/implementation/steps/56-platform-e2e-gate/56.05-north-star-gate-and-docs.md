# Step 56.05: North-star gate + docs

## Status

Not started

## Epic

`56-platform-e2e-gate` → [`../../epics/56-platform-e2e-gate.md`](../../epics/56-platform-e2e-gate.md)

## Global sequence (N)

**`N = 216`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `56.05`

## Goal

Declare `make test-platform-e2e` the verification track's north-star gate: one command deploys and
browser-drives all five products, verifies full service coverage, and emits a consolidated findings
report — with a runbook and top-level docs so anyone can run it.

## Dependencies

* `56.01`–`56.04`; epics 50–55 complete.

## Scope

* Final acceptance: a green full run headed and headless with a complete coverage table and an
  up-to-date `PLATFORM_FINDINGS.md`.
* Runbook (`docs/demo-projects/RUNBOOK.md`): prerequisites, run modes, reading the report, handling
  findings, troubleshooting.
* README/roadmap/progress finalized for the track; epic 56 → Complete.

## Explicitly out of scope

* Any platform fix for recorded findings.

## Implementation tasks

1. Run the full suite headed + headless; confirm the gate semantics.
2. Write the runbook.
3. Finalize track docs + statuses.

## Files to create

* `docs/demo-projects/RUNBOOK.md`

## Files to modify

* `docs/demo-projects/README.md`; `docs/implementation/roadmap.md`; `progress.md`;
  `docs/implementation/epics/56-platform-e2e-gate.md` (Complete).

## Unit / integration tests

* Covered by the full suite run.

## Demo scenario

```text
make test-platform-e2e → platform up → demos 1..5 (browser) → coverage 19/19
  → consolidated PLATFORM_FINDINGS.md → exit 0
```

## Manual verification

```bash
make test-platform-e2e
make test-platform-e2e HEADLESS=1
```

## Acceptance criteria

* Full suite passes headed + headless; coverage complete; findings consolidated; runbook published;
  epic 56 and the verification track marked Complete.

## Expected Git commit

```text
test(56.05): finalize platform e2e north-star gate and docs
```
