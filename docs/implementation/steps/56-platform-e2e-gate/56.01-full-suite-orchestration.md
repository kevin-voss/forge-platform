# Step 56.01: Full-suite orchestration

## Status

Complete

## Epic

`56-platform-e2e-gate` → [`../../epics/56-platform-e2e-gate.md`](../../epics/56-platform-e2e-gate.md)

## Global sequence (N)

**`N = 212`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `56.01`

## Goal

Make `make test-platform-e2e` (no `PROJECTS`) run all five demo products in numeric order through
the shared lifecycle, with a single aggregate result and exit code.

## Dependencies

* Epics 50–55 complete.

## Scope

* Default (no `PROJECTS`) = run `01,02,03,04,05` in order.
* Per-product pass/degraded/fail rollup; continue-on-degraded, stop-eligible-on-blocker semantics.
* Exit 0 iff all products pass and zero blocker findings.

## Explicitly out of scope

* Coverage gate (56.02); consolidation (56.03).

## Implementation tasks

1. Extend the orchestrator default set + ordering.
2. Implement aggregate result + exit-code rules.
3. Verify a full run against the platform.

## Files to modify

* `tests/e2e/harness/orchestrator.ts`; `Makefile`; `progress.md`.

## Unit / integration tests

* Full run returns exit 0 when all pass; non-zero when a product blocks.

## Demo scenario

`make test-platform-e2e` runs all five in order and reports one aggregate result.

## Manual verification

```bash
make test-platform-e2e
make test-platform-e2e HEADLESS=1
```

## Acceptance criteria

* All five products run in order with correct aggregate pass/fail + exit code.

## Expected Git commit

```text
test(56.01): run full demo suite via test-platform-e2e
```
