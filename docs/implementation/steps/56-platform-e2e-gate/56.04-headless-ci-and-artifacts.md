# Step 56.04: Headless/CI mode + artifacts

## Status

Complete

## Epic

`56-platform-e2e-gate` → [`../../epics/56-platform-e2e-gate.md`](../../epics/56-platform-e2e-gate.md)

## Global sequence (N)

**`N = 215`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `56.04`

## Goal

Make the full suite run cleanly in CI headless, publish artifacts, and support a fast PR subset vs a
full nightly run.

## Dependencies

* `56.01`–`56.03`.

## Scope

* `CI=1` implies `HEADLESS=1`, `KEEP=0`; uploads `tests/e2e/artifacts/` (traces, videos, report,
  `findings.json`, `PLATFORM_FINDINGS.md`).
* `CI_SUBSET` (e.g. `PROJECTS=01,03`) for PR gating; full five on nightly (mirrors capstone pattern).
* A CI workflow doc/example (GitHub Actions) — no secret required for fake-backed demos.

## Explicitly out of scope

* North-star docs (56.05).

## Implementation tasks

1. Implement CI env semantics + artifact collection.
2. Document PR-subset vs nightly-full.
3. Provide an example CI workflow.

## Files to create

* `.github/workflows/platform-e2e.yml` (example) or documented equivalent.

## Files to modify

* orchestrator (CI semantics); `Makefile`; `docs/demo-projects/e2e-harness.md`; `progress.md`.

## Unit / integration tests

* `CI=1 make test-platform-e2e HEADLESS=1 CI_SUBSET=1` runs the subset and uploads artifacts.

## Demo scenario

CI runs the subset headless, uploads a report + findings + traces.

## Manual verification

```bash
CI=1 HEADLESS=1 make test-platform-e2e PROJECTS=01,03
```

## Acceptance criteria

* Full suite runs headless in CI with artifacts uploaded; PR-subset and nightly-full both supported.

## Expected Git commit

```text
test(56.04): add headless CI mode and artifact publishing
```
