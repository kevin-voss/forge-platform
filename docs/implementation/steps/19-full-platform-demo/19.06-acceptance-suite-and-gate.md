# Step 19.06: `demos/09-full-platform` acceptance suite + docs

## Status

Complete

## Epic

`19-full-platform-demo` → [`../../epics/19-full-platform-demo.md`](../../epics/19-full-platform-demo.md)

## Global sequence (N)

**`N = 131`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md).

Internal id (file/commit): `19.06`

## Goal

Package the entire capstone so one root command starts the whole demo and one command runs an acceptance suite that proves the north-star recovery scenario end to end, and finalize documentation. This is the platform's north-star gate.

## Why this step exists

`specs.md` Step 19 acceptance requires one command to start the demo and one command to execute the acceptance suite, with all services communicating only through documented contracts. This is the final proof the platform is "100% working".

## Dependencies

* `19.05` (full recovery loop working manually)
* Every prior epic's gate green

## Scope

* One-command start of the full demo (`demos/09-full-platform`)
* One-command acceptance suite covering the `specs.md` Step 19 test list
* Contract-only communication verification
* Capstone documentation (README, runbook, architecture, scenario walkthrough)
* Wire into the demo runner and (documented) CI subset
* Reconfirm folder naming: capstone `demos/09-full-platform` vs Identity `demos/09-platform-identity`

## Explicitly out of scope

* New product/platform features
* Performance/scale hardening beyond the scenario

## Architecture

```text
demos/09-full-platform/
├── compose.yaml            # full platform + product
├── start.sh                # one command: bring everything up healthy
├── accept.sh               # one command: run the acceptance suite
├── tests/                  # the Step 19 test list
├── runbook.md
└── README.md
```

## Components

| Component | Purpose |
|---|---|
| `start.sh` | Single command to start the whole demo |
| `accept.sh` | Single command acceptance suite |
| `tests/` | Smoke/deploy/identity/secrets/events/telemetry/model/agent/workflow/rollback/interop |
| Docs | README + runbook + scenario walkthrough |

## Contracts

* Acceptance asserts services communicate only through documented contracts (no back-door coupling)
* Uses each service's finalized OpenAPI/event schemas

## Data model

None new; validates the assembled system.

## Configuration

```text
DEMO=09-full-platform          # capstone selector (documented alias allowed)
CI_SUBSET=true|false           # full stack vs documented CI subset
FORGE_MODELS_BACKEND=fake      # deterministic CI
```

## Security considerations

* Enforced Identity; no hardcoded secrets; masked logs
* Rollback only via approval
* All actions auditable

## Observability

* `accept.sh` prints per-test pass/fail + the recovery trace/audit
* On failure dumps correlated logs across services

## Failure handling

* Robust wait-for-healthy before tests
* Any failed assertion → non-zero exit; teardown always runs
* Documented CI subset if full-stack build time is prohibitive

## Implementation tasks

1. Author `start.sh` (bring up full platform + product to healthy).
2. Implement `accept.sh` + `tests/` covering the Step 19 test list:
   - complete platform smoke, full deployment, identity/permission, secret-injection,
     event-processing, telemetry, model-serving, agent-tool, workflow recovery, rollback, multi-language interop
3. Add contract-only communication verification.
4. Write README, runbook, and scenario walkthrough; reconfirm folder-naming note.
5. Wire the demo runner + documented CI subset gate.
6. Update `docs/implementation/` roadmap/README/progress to reflect north-star completion.

## Files to create

* `demos/09-full-platform/start.sh`
* `demos/09-full-platform/accept.sh`
* `demos/09-full-platform/tests/` (test scripts per acceptance item)
* `demos/09-full-platform/runbook.md`

## Files to modify

* `demos/09-full-platform/README.md`
* `Makefile` (capstone selector/alias)
* `docs/implementation/progress.md` (epic 19 → Complete; north-star checklist)
* `docs/implementation/epics/19-full-platform-demo.md`
* `docs/implementation/roadmap.md` / `README.md` / `MASTER_PLAN.md` status pointers

## Unit tests

* Acceptance helper scripts (self-checks, optional)

## Integration tests

* `start.sh` brings the full stack + product to healthy
* `accept.sh` runs the entire Step 19 test list green

## Contract tests

* Every inter-service call validated against documented contracts
* All service OpenAPIs/event schemas valid

## Demo scenario

```text
one command:  ./start.sh            # entire demo up
one command:  ./accept.sh           # runs full acceptance suite
proves: deploy → run → broken release → detect → workflow → agent diagnosis
        (telemetry + memory) → human approval → rollback → report → healthy again
```

## Manual verification

```bash
cd demos/09-full-platform
./start.sh
./accept.sh
echo "exit=$?"   # 0 on success
```

## Acceptance criteria

* One root command starts the entire demo; one command executes the acceptance suite (exit `0`)
* All services communicate only through documented contracts
* A broken deployment is detected and recovered; the agent diagnosis references real telemetry; approval is required before rollback; all actions are auditable
* The full Step 19 test list passes (smoke, deploy, identity, secrets, events, telemetry, models, agents, workflow recovery, rollback, multi-language interop)
* Capstone lives in `demos/09-full-platform`; `demos/09-platform-identity` is untouched and distinct
* Docs (README/runbook/roadmap/progress) reflect north-star completion
* Epic 19 (and the north-star checklist) marked Complete only after this gate passes

## Definition of done

* Acceptance criteria pass
* Docs updated
* `progress.md` updated
* One focused Git commit

## Expected Git commit

```text
feat(19.06): add full-platform acceptance suite and north-star gate
```
