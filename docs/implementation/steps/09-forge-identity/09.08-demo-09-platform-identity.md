# Step 09.08: Demo `09-platform-identity` + epic gate

## Status

Complete

## Epic

`09-forge-identity` → [`../../epics/09-forge-identity.md`](../../epics/09-forge-identity.md)

## Global sequence (N)

**`N = 63`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md).

Internal id (file/commit): `09.08`

## Goal

Assemble `demos/09-platform-identity` proving the full identity story end to end: create user → org → project → developer token → authorized deploy → viewer denied → revoke token → access fails. Wire it into `make demo DEMO=09` as the epic acceptance gate, running against Control in enforce mode.

## Why this step exists

The identity pieces (users, sessions, roles, tokens, enforcement, CLI login) only prove the epic when a single command demonstrates authentication, role enforcement, and revocation against the real secured stack. This is the gate in `MASTER_PLAN.md`.

## Dependencies

* Steps `09.01`–`09.07` — full identity service, Control enforcement, CLI login
* Epics `02-forge-control`, `04-forge-runtime` (for the deploy step) under Compose

## Scope

* `demos/09-platform-identity/` Compose overlay: Identity + Control (enforce mode) + Runtime + demo service
* Acceptance script executing the 8-step scenario with assertions on status codes
* `make demo DEMO=09` wiring + README
* Uses `forge` CLI (`09.07`) for the human-facing steps and direct API calls where clearer

## Explicitly out of scope

* New identity features (all in 09.01–09.07)
* Gateway auth
* Load/perf testing of introspection

## Architecture

```text
demos/09-platform-identity/
├── docker-compose.yml   # identity, control(enforce), runtime, demo svc
├── run.sh               # 8-step scenario + assertions
└── README.md
```

## Components

| Component | Responsibility |
|---|---|
| `run.sh` | Drive create→deploy→deny→revoke→fail; assert codes |
| Identity + enforced Control | Real auth path |
| Demo service | Contract-compliant deploy target |

## Contracts

* Uses Identity APIs (09.02–09.05), Control deployment API under enforcement (09.06), CLI login (09.07)
* No new contracts

## Data model

None new. Demo reads token/introspection state for assertions.

## Configuration

```text
DEMO=09
FORGE_AUTH_MODE=enforce      # the point of the demo
FORGE_INTROSPECT_CACHE_TTL_S=2   # short so revocation is quick in the demo
```

## Security considerations

* Demo runs in enforce mode (no bypass) — this is the security acceptance
* Test passwords/tokens are ephemeral; README warns not to reuse
* Assert no secrets/passwords appear in service logs

## Observability

* `run.sh` prints each step + status code; on failure dumps Identity + Control logs
* Asserts logs contain no plaintext passwords/token secrets

## Failure handling

* Any step returning an unexpected status → demo fails with diagnostics
* Cleanup trap tears down Compose + clears the CLI profile on exit

## Implementation tasks

1. Write `docker-compose.yml` with Identity + Control(enforce) + Runtime + demo service.
2. Write `run.sh`: create user/org/project; issue developer + viewer tokens; deploy as developer (expect success); deploy as viewer (expect 403); revoke developer token; retry (expect 401).
3. Assert no plaintext secrets in logs.
4. Add `make demo DEMO=09` + README documenting the scenario and enforce-mode requirement.

## Files to create

* `demos/09-platform-identity/docker-compose.yml`
* `demos/09-platform-identity/run.sh`
* `demos/09-platform-identity/README.md`

## Files to modify

* `Makefile` (dispatch `DEMO=09`)
* `docs/implementation/progress.md`
* `docs/implementation/epics/09-forge-identity.md` (mark epic gate + note dev bypass removed by default)

## Unit tests

* None (orchestration). Underlying behavior covered by 09.01–09.07 tests.

## Integration tests

* `run.sh` is the integration test; runnable in CI
* Optional split flags per scenario phase for targeted CI

## Contract tests

* Demo service passes the epic-01 runtime contract validator
* Token/introspection/authz calls conform to Identity OpenAPI

## Demo scenario

```text
1 create user            2 create org            3 create project
4 issue developer token  5 deploy (developer)=200
6 deploy (viewer)=403    7 revoke developer token
8 deploy (revoked)=401   → PASS
```

## Manual verification

```bash
make demo DEMO=09
# expected tail:
#   [4] developer token issued
#   [5] deploy as developer -> 201 OK
#   [6] deploy as viewer -> 403 OK
#   [7] revoked developer token
#   [8] deploy with revoked token -> 401 OK
#   logs contain plaintext secret: no
#   demo 09 PASSED
echo "exit=$?"   # expect 0
```

## Acceptance criteria

* `make demo DEMO=09` runs the 8-step scenario and exits 0 with Control in enforce mode
* Unauthenticated/revoked requests are rejected (`401`); viewer is denied deploy (`403`); developer deploy succeeds
* Token revocation takes effect promptly (within the short introspection cache TTL)
* No plaintext passwords or token secrets appear in service logs
* Demo service satisfies the epic-01 runtime contract; stack tears down on exit

## Definition of done

* Acceptance criteria pass
* Docs updated
* `progress.md` updated
* One focused Git commit

## Expected Git commit

```text
feat(09.08): add platform-identity demo and epic 09 acceptance gate
```
