# Step 51.01: Product scaffold + baseline deploy

## Status

Complete

## Epic

`51-demo-taskflow` → [`../../epics/51-demo-taskflow.md`](../../epics/51-demo-taskflow.md)

## Global sequence (N)

**`N = 181`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `51.01`

## Goal

Create the TaskFlow product (Go API + minimal SPA) and deploy it onto Forge from source, reachable
through the Gateway at `app.taskflow.localhost` / `api.taskflow.localhost`.

## Dependencies

* Epic 50 harness; Build, Control, Runtime, Gateway, `forge apply`.

## Scope

* `demos/51-taskflow/`: `taskflow-api` (Go, `/health/ready`, `/tasks` CRUD stub), `taskflow-web`
  (shared minimal SPA), Dockerfiles, `forge.yaml` build manifest + Application/Route resource docs.
* `run.sh` (`forge build` + `forge apply` + `forge wait Ready`, `--down` teardown), `demo.json`.

## Explicitly out of scope

* Database, identity, secrets, observability assertions (later steps) — API may use in-memory store
  until 51.02.

## Implementation tasks

1. Scaffold API + SPA + Dockerfiles.
2. Author build manifest + Application/Route docs (two apps).
3. Implement `run.sh` deploy/teardown + `demo.json`.
4. Verify both hosts return 200 through the Gateway.

## Files to create

* `demos/51-taskflow/**` (api, web, Dockerfiles, forge.yaml, run.sh, demo.json, README.md)

## Files to modify

* `Makefile` (demo 51 wiring if needed); `docs/implementation/progress.md`.

## Unit / integration tests

* API health + basic `/tasks` handler unit test.

## Demo scenario

Deploy → both hosts reachable through Gateway; `forge get applications` shows Ready.

## Manual verification

```bash
make demo DEMO=51
curl -fsS -H 'Host: api.taskflow.localhost' http://127.0.0.1:4000/health/ready
curl -fsS -H 'Host: app.taskflow.localhost' http://127.0.0.1:4000/
```

## Acceptance criteria

* TaskFlow builds from source and deploys to Ready; both hosts serve through the Gateway.

## Expected Git commit

```text
test(51.01): scaffold TaskFlow product and baseline deploy
```
