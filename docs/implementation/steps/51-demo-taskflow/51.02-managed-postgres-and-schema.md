# Step 51.02: Managed Postgres + schema

## Status

Complete

## Epic

`51-demo-taskflow` → [`../../epics/51-demo-taskflow.md`](../../epics/51-demo-taskflow.md)

## Global sequence (N)

**`N = 182`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `51.02`

## Goal

Give TaskFlow a **managed** Postgres database via a typed dependency, run migrations, and persist
tasks across restarts.

## Dependencies

* `51.01`; managed PostgreSQL (epic 18).

## Scope

* Add `dependencies.database { type: postgres, name: taskflow-db }` to the API Application.
* Migrations for `users`, `projects`, `tasks` (idempotent, run on boot).
* Wire the API to the injected connection info; move `/tasks` off in-memory to SQL.
* `seed.sh`: admin + member user, one shared project, two open tasks (idempotent).

## Explicitly out of scope

* Identity/secrets (51.03/51.04) — DB URL may be plaintext env *only* until 51.04, then moved to
  Secrets.

## Implementation tasks

1. Declare the database dependency; wait for the `Database` resource Ready.
2. Implement migrations + SQL-backed task store.
3. Implement `seed.sh`.
4. Prove persistence across an API container restart.

## Files to create

* `demos/51-taskflow/migrations/**`, `demos/51-taskflow/seed.sh`

## Files to modify

* API source; Application resource doc; `demo.json` (`seed`); `progress.md`.

## Unit / integration tests

* Repository-layer test against a Postgres test container (CRUD tasks).

## Demo scenario

Create a task → restart API container → task still present (managed DB persistence).

## Manual verification

```bash
make demo DEMO=51
# create a task via API, restart the api container, re-list → task persists
```

## Acceptance criteria

* `Database` reaches Ready; migrations apply; tasks persist across restart; seed is idempotent.

## Expected Git commit

```text
test(51.02): add managed Postgres dependency and schema to TaskFlow
```
