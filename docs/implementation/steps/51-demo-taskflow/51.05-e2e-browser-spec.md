# Step 51.05: E2E browser spec

## Status

Complete

## Epic

`51-demo-taskflow` → [`../../epics/51-demo-taskflow.md`](../../epics/51-demo-taskflow.md)

## Global sequence (N)

**`N = 185`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `51.05`

## Goal

Write the Playwright spec that drives TaskFlow through the browser (signup → login → create/persist/
complete tasks → role gating) with product and platform assertions.

## Dependencies

* `51.01`–`51.04`; harness (epic 50).

## Scope

* `tests/e2e/projects/01-taskflow/spec.ts` per
  [`projects/01-taskflow.md` §6](../../../demo-projects/projects/01-taskflow.md).
* Product assertions: signup, login, create task, reload-persists, toggle done, admin-only control
  hidden from member.
* Platform assertions (`platform.expect`): Identity introspect claims/roles, no plaintext secrets,
  managed DB persistence across restart, ≥1 trace for `POST /tasks` in Observe.

## Explicitly out of scope

* Demo wiring / gate (51.06).

## Implementation tasks

1. Author the spec with fresh unique users per run.
2. Add the four platform assertions.
3. Confirm headed (visible) and headless pass.

## Files to create

* `tests/e2e/projects/01-taskflow/spec.ts`

## Files to modify

* `demo.json` (`spec`, `services`); `progress.md`.

## Unit / integration tests

* The spec itself is the integration test.

## Demo scenario

Watch the browser sign up, log in, create and complete "Buy milk", and hide admin controls from a
member.

## Manual verification

```bash
cd tests/e2e && npx playwright test projects/01-taskflow
HEADLESS=1 npx playwright test projects/01-taskflow
```

## Acceptance criteria

* Spec passes headed and headless; platform assertions record findings on failure rather than
  hard-failing where appropriate.

## Expected Git commit

```text
test(51.05): add TaskFlow browser e2e spec
```
