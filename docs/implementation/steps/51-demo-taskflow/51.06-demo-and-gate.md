# Step 51.06: Demo + epic gate

## Status

Complete

## Epic

`51-demo-taskflow` → [`../../epics/51-demo-taskflow.md`](../../epics/51-demo-taskflow.md)

## Global sequence (N)

**`N = 186`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `51.06`

## Goal

Wire TaskFlow into `make demo DEMO=51` and the `test-platform-e2e` orchestrator; make the demo pass
end-to-end headed and headless; mark epic 51 complete.

## Dependencies

* `51.01`–`51.05`.

## Scope

* Finalize `demo.json` (hosts, deploy, seed, spec, services, teardown).
* Ensure `make demo DEMO=51` runs the full lifecycle and exits 0.
* Ensure `make test-platform-e2e PROJECTS=01` runs TaskFlow through the orchestrator.
* Document any platform findings surfaced (do not patch services).

## Explicitly out of scope

* Aggregate/coverage gate (epic 56).

## Implementation tasks

1. Finalize `demo.json` + `run.sh`/`seed.sh`.
2. Verify both run paths (single demo + orchestrator) pass headed and headless.
3. Confirm findings (if any) are recorded, not fixed.
4. Update epic + progress status.

## Files to create

* `demos/51-taskflow/README.md` (runbook) if not already present.

## Files to modify

* `docs/implementation/epics/51-demo-taskflow.md` (Complete); `progress.md`; coverage matrix note.

## Unit / integration tests

* Covered by the demo + orchestrator run.

## Demo scenario

`deploy → seed → browser e2e → assert → findings → teardown` all green.

## Manual verification

```bash
make demo DEMO=51
make test-platform-e2e PROJECTS=01
make test-platform-e2e HEADLESS=1 PROJECTS=01
```

## Acceptance criteria

* TaskFlow passes headed and headless via both `make demo DEMO=51` and the orchestrator.
* Zero blocker findings attributed to TaskFlow; epic 51 marked Complete.

## Expected Git commit

```text
test(51.06): wire TaskFlow demo and epic 51 gate
```
