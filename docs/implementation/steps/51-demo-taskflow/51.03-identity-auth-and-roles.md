# Step 51.03: Identity auth + roles

## Status

Not started

## Epic

`51-demo-taskflow` → [`../../epics/51-demo-taskflow.md`](../../epics/51-demo-taskflow.md)

## Global sequence (N)

**`N = 183`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `51.03`

## Goal

Protect TaskFlow with Forge Identity: signup/login issue a PAT, the API validates Bearer tokens via
Identity introspect, app roles (`admin`/`member`) gate actions, and deploy-time RBAC is proven
(viewer PAT → 403, developer PAT → 200).

## Dependencies

* `51.02`; Identity (epic 09).

## Scope

* Signup/login endpoints; exchange a valid credential for a PAT (and/or app JWT) per the platform's
  auth pattern (see epic open question).
* API middleware: Bearer → Identity introspect → attach identity + role; reject invalid/expired.
* App-role gating: `admin`-only "delete project"; `member` cannot.
* Deploy RBAC proof captured in `run.sh`/`seed.sh`: `forge deployment create` with a **viewer** PAT
  returns 403, **developer** PAT succeeds.

## Explicitly out of scope

* Secret storage of the JWT signing key (51.04).

## Implementation tasks

1. Implement signup/login + PAT issuance and introspect middleware.
2. Implement role gating in API + SPA.
3. Add the viewer/developer deploy RBAC checks.
4. Record any Identity contract gap as a finding (do not patch Identity).

## Files to create

* auth middleware + handlers under `demos/51-taskflow/`.

## Files to modify

* API + SPA; `run.sh` (RBAC checks); `progress.md`.

## Unit / integration tests

* Introspect middleware: valid/invalid/expired token cases.
* Role gate: admin allowed, member denied on protected action.

## Demo scenario

Login as member → no delete control; login as admin → delete works; viewer deploy → 403.

## Manual verification

```bash
# developer PAT deploy succeeds; viewer PAT deploy → 403
forge deployment create ... # with each PAT
```

## Acceptance criteria

* Bearer auth via Identity works; role gating holds; viewer 403 / developer 200 on deploy.

## Expected Git commit

```text
test(51.03): add Identity auth and role gating to TaskFlow
```
