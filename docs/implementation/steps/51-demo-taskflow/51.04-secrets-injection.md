# Step 51.04: Secrets injection

## Status

Not started

## Epic

`51-demo-taskflow` → [`../../epics/51-demo-taskflow.md`](../../epics/51-demo-taskflow.md)

## Global sequence (N)

**`N = 184`** — use this number in [`IMPLEMENT_STEP.md`](../../IMPLEMENT_STEP.md). Verification & demo-projects track (epics 50–56, `N = 174`–`216`); continues after the platform queue (`N ≤ 173`).

Internal id (file/commit): `51.04`

## Goal

Move the DB connection string and JWT signing key into Forge Secrets and inject them as env — with
no plaintext secrets in manifests or logs.

## Dependencies

* `51.03`; Secrets (epic 10).

## Scope

* Create secrets `taskflow/db-url`, `taskflow/jwt-key`; reference them via `valueFrom.secret` in the
  Application env.
* Remove any plaintext DB URL / signing key from manifests and app logs.
* Add a check (in `run.sh` and the E2E platform assertions) that the rendered manifest and container
  logs contain no secret values.

## Explicitly out of scope

* Secret rotation flows (record as a finding/enhancement if the platform lacks refresh semantics).

## Implementation tasks

1. Provision the secrets during `run.sh`.
2. Switch env to secret refs.
3. Add no-plaintext-leak assertions.

## Files to create

* secret-provisioning snippet in `run.sh`.

## Files to modify

* Application resource doc; API config loading; `progress.md`.

## Unit / integration tests

* Config loader reads injected env; boot fails clearly if a required secret is absent.

## Demo scenario

App boots with secrets injected; `grep` of manifest + logs finds no secret values.

## Manual verification

```bash
make demo DEMO=51
# assert DATABASE_URL / JWT key present in container env, absent from manifest + logs
```

## Acceptance criteria

* Secrets injected from Forge Secrets; zero plaintext secrets in manifest/logs; app healthy.

## Expected Git commit

```text
test(51.04): inject TaskFlow secrets via Forge Secrets
```
