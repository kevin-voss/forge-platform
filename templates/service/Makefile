.PHONY: dev build run test test-unit test-integration lint format docker-build docker-run clean

dev:
	@echo "Implement service-specific hot reload in a later step."

build:
	@echo "Implement service build."

run:
	@echo "Implement service run."

test: test-unit test-integration

test-unit:
	@echo "No unit tests yet."

test-integration:
	@echo "No integration tests yet."

lint:
	@echo "No linter configured yet."

format:
	@echo "No formatter configured yet."

docker-build:
	@echo "No Dockerfile build yet."

docker-run:
	@echo "No docker run yet."

clean:
	@rm -rf dist build target .tmp
