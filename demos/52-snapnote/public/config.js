// Filled at deploy time by run.sh (project slug is unique per demo run).
window.SNAPNOTE_CONFIG = {
  projectSlug: '',
  environment: 'local',
  workerPolicy: 'snapnote-worker-queue',
  minReplicas: 1,
  maxReplicas: 8,
};
